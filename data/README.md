# Log Ghost — site & product data

`logghost.json` is the **single source of copy and structure** for the marketing site (see `../Log-Ghost-Website`).

Update fields here, then rebuild or refresh the Next.js app. Add your real **Marketplace** item URL in `meta` and CTAs when the extension is published.
